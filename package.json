{
  "name": "paymentgateway_react_stripe",
  "version": "1.0.0",
  "description": "This package.json is to run scripts on server and client projects",
  "scripts": {
    "install": "node ./scripts/InstallDependencies.js",
    "start": "node ./scripts/Start.js"
  }
}
